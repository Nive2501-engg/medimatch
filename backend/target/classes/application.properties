# Server
server.port=8081

# MySQL
spring.datasource.url=jdbc:mysql://localhost:3306/medimatch?useSSL=false&allowPublicKeyRetrieval=true&serverTimezone=UTC
spring.datasource.username=root
spring.datasource.password=Nivetha2501
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# JPA
spring.jpa.hibernate.ddl-auto=none
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Endee Vector Database
endee.base-url=http://localhost:8080/api/v1
endee.auth-token=
endee.doctors-index=doctors_index
endee.medicines-index=medicines_index
endee.top-k=5

# CORS
app.cors.allowed-origins=*

# ── OpenAI (for RAG generation step) ─────────────────────────
openai.api-key=sk-your-openai-key-here
openai.model=gpt-3.5-turbo
# Leave openai.api-key empty to use built-in fallback (no cost)